[tox]
min_version = 2.0
env_list = py312-{win32,linux,mac}

[testenv]
skip_install = False
commands = pytest --cov-branch --cov-append --cov=chickenstats
deps =
    pytest
    pytest-cov
package = editable





